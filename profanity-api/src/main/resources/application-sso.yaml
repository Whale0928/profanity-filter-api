spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_OAUTH_CLIENT_ID:stub-github-client}
            client-secret: ${GITHUB_OAUTH_CLIENT_SECRET:stub-github-secret}
            redirect-uri: ${API_SERVER_URL:http://localhost:8080}/login/oauth2/code/{registrationId}
            scope:
              - read:user
              - user:email
          google:
            client-id: ${GOOGLE_OAUTH_CLIENT_ID:stub-google-client}
            client-secret: ${GOOGLE_OAUTH_CLIENT_SECRET:stub-google-secret}
            redirect-uri: ${API_SERVER_URL:http://localhost:8080}/login/oauth2/code/{registrationId}
            scope:
              - openid
              - profile
              - email
        provider:
          github:
            authorization-uri: ${GITHUB_OAUTH_AUTHORIZATION_URI:http://127.0.0.1:65535/oauth2/authorize}
            token-uri: ${GITHUB_OAUTH_TOKEN_URI:http://127.0.0.1:65535/oauth2/token}
            user-info-uri: ${GITHUB_OAUTH_USER_INFO_URI:http://127.0.0.1:65535/user}
            user-name-attribute: id
          google:
            authorization-uri: ${GOOGLE_OAUTH_AUTHORIZATION_URI:http://127.0.0.1:65535/oauth2/authorize}
            token-uri: ${GOOGLE_OAUTH_TOKEN_URI:http://127.0.0.1:65535/oauth2/token}
            user-info-uri: ${GOOGLE_OAUTH_USER_INFO_URI:http://127.0.0.1:65535/userinfo}
            jwk-set-uri: ${GOOGLE_OAUTH_JWK_SET_URI:http://127.0.0.1:65535/oauth2/certs}
            user-name-attribute: sub

sso:
  frontend:
    redirect-uri: ${SSO_FRONTEND_REDIRECT_URI:http://localhost:5173/login}
  cookie:
    name: PF_OAUTH2_AUTHORIZATION_REQUEST
    ttl-seconds: 300
    signing-key: ${SSO_COOKIE_SIGNING_KEY:stub-sso-cookie-signing-key-for-local-and-ci}

security:
  login:
    exchange-code-ttl: ${LOGIN_EXCHANGE_CODE_TTL:60s}
    refresh-token-ttl: ${LOGIN_REFRESH_TOKEN_TTL:14d}
    absolute-session-ttl: ${LOGIN_ABSOLUTE_SESSION_TTL:30d}
    refresh-reuse-grace: ${LOGIN_REFRESH_REUSE_GRACE:5s}
    allowed-origins: ${SSO_FRONTEND_ALLOWED_ORIGINS:http://localhost:5173}
    refresh-cookie:
      name: ${LOGIN_REFRESH_COOKIE_NAME:PF_LOGIN_REFRESH}
      secure: ${LOGIN_REFRESH_COOKIE_SECURE:false}
      same-site: ${LOGIN_REFRESH_COOKIE_SAME_SITE:Strict}
      path: /api/v1/auth

app:
  security:
    jwt:
      issuer: ${LOGIN_JWT_ISSUER:http://localhost:8080}
      audience: ${LOGIN_JWT_AUDIENCE:profanity-dashboard}
      access-token-ttl: ${LOGIN_JWT_ACCESS_TTL:15m}
      clock-skew: ${LOGIN_JWT_CLOCK_SKEW:30s}
      keys:
        private-jwk: ${LOGIN_JWT_PRIVATE_JWK:}
        public-jwk-set: ${LOGIN_JWT_PUBLIC_JWK_SET:}
