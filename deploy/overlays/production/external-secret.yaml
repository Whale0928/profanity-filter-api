apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: profanity-secrets
  namespace: profanity-production
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword-store
    kind: SecretStore
  target:
    name: profanity-secrets
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: profanity-api-production
